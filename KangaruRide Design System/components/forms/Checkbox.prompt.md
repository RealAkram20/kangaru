18px square checkbox, green when checked; also the row-select control in DataTable.

```jsx
<Checkbox label="Return trip" description="Adds a second leg at the same rate" />
<Checkbox indeterminate />
```

Use for multi-select and opt-ins. For a single on/off setting that applies immediately, use `Switch`.
