An immediate-effect toggle: notification channels, geofence enforcement, driver availability, night-rate rules.

```jsx
<Switch defaultChecked label="Flag odometer/GPS variance" />
```

Switches apply on flip — no Save button. If the change needs confirming, use a Checkbox in a form instead.
