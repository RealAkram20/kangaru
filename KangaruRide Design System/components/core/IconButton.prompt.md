A square icon-only control for chrome, toolbars and table row actions.

```jsx
<IconButton icon="bell" label="Notifications" onChrome />
<IconButton icon="download" label="Export" variant="outline" size="sm" />
```

`label` is mandatory. Minimum 32px box in tables, 40px elsewhere; on touch surfaces (driver web) use `lg`.
