A navy hover/focus label for truncated data and terse column headers.

```jsx
<Tooltip label="GPS distance vs odometer distance"><Icon name="info" size={14} /></Tooltip>
```

Tooltips explain, they never carry information the user needs to complete a task. Fades in over 120ms; no scale or bounce.
