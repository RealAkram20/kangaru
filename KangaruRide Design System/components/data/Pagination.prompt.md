The footer bar for every paged table; sits inside a `padding="none"` Card directly under the DataTable.

```jsx
<Pagination page={1} pageCount={8} pageSize={25} total={184} onPageChange={setPage} />
```

Counts use tabular figures. Prev/next only — no numbered page list; operational lists are filtered, not browsed.
